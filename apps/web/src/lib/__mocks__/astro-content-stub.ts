// Stub for astro:content — replaced by vi.mock() in individual tests.
// This file exists only so vitest can resolve the alias at startup.
export const getCollection = async () => [];
export const getEntry = async () => undefined;
