{
  "name": "@boardstack/shared",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./tokens.css": "./src/tokens.css"
  },
  "scripts": {
    "knowledge:check": "tsx scripts/generate-knowledge.ts --check",
    "knowledge:generate": "tsx scripts/generate-knowledge.ts",
    "lint": "pnpm exec eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "zod": "^3.24"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.3",
    "typescript": "^5.7",
    "vitest": "^4.1.3"
  }
}
